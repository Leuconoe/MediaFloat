# IzzyOnDroid metadata notes

MediaFloat reuses the Fastlane-compatible metadata in `fastlane/metadata/android/en-US/` for IzzyOnDroid.

Key points for IzzyOnDroid:

- package id: `sw2.io.mediafloat`
- signing continuity must be preserved across releases
- use the same changelog and screenshot set as the Fastlane metadata tree
- point release notes at `docs/releases/v0.2.1.md`
